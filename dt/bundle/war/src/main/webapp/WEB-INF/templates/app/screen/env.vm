/**********************
* APP ENV properties
**********************/
#foreach( $entry in ${env.entrySet()}) 	
	<br>${entry.key} = ${entry.value}<br>
#end 
/**********************
* JVM ENV properties
**********************/
#foreach( $entry in ${sysEnv.entrySet()}) 	
	<br>${entry.key} = ${entry.value}<br>
#end 
/**********************
* System properties
**********************/
#foreach( $entry in ${sysProp.entrySet()}) 	
	<br>${entry.key} = ${entry.value}<br>
#end 


